# Rust tools

Add one directory per Rust CLI under this directory. Each tool directory keeps
its `Cargo.toml`, `Cargo.lock`, README, and `package.nix` together.

Expose completed tools through the root `nix/tools.nix` registry.
