# Bun tools

Add one directory per Bun/npm CLI under this directory. Each tool directory
keeps its `package.json`, Bun lockfile, README, and `package.nix` together.

Expose completed tools through the root `nix/tools.nix` registry.
