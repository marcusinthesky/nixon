# Python tools

Add one directory per Python CLI under this directory. Each tool directory
keeps its `pyproject.toml`, `uv.lock`, README, and `package.nix` together.

Expose completed tools through the root `nix/tools.nix` registry.
